These endpoints were moved here to avoid hitting Vercel Hobby plan limit (max 12 serverless functions).

Moved endpoints are not deployed because they are not at the root of `api/`.

If you upgrade to Pro or want to re-enable any endpoint, move the file back to `api/`.
