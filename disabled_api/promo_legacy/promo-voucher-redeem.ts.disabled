This endpoint has been superseded by `api/voucher-redeem.ts` which now redeems both normal vouchers and promo vouchers.

It is intentionally disabled to reduce the number of deployed serverless functions.

Previously: redeemed promo vouchers by voucher_id.
Now: use POST /api/voucher-redeem with { code }.
